# NAGPUR-R2 Integrated Backend — Python dependencies
# Run: pip install -r requirements.txt  (inside a fresh venv)
#
# NOTE: M1's heavy CV deps (TensorFlow, OpenCV, Ultralytics) are NOT here.
# M1 runs as a separate process and has its own requirements.txt.

# --- Backend / API orchestration ---
fastapi==0.141.1
uvicorn[standard]==0.52.3
pydantic==2.13.4
python-dotenv==1.2.2

# --- M2: Risk Propagation + Risk Shadow ---
networkx==3.6.1
numpy==2.5.2

# --- M3: Police Deployment (CP-SAT) ---
ortools==9.15.6755

# --- Testing ---
pytest==9.1.1
httpx==0.28.1
