# Backend Server
PORT=8000
HOST=0.0.0.0
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# M2 Propagation
RISK_SHADOW_THRESHOLD=70
PROPAGATION_ALPHA=0.3

# M3 Deployment
DEPLOYMENT_CANDIDATE_MIN_FUTURE_RISK=60
OPTIMIZER_TIME_LIMIT_SECONDS=5
DEFAULT_UNIT_COUNT=10

# XAI
XAI_AUDIT_LOG_PATH=data/audit_log.json
